[project]
name = "books"
version = "0.1.0"
description = "Kindle等のスクリーンショット連続撮影 & MEGAアップロードツール (Hyprland専用)"
requires-python = ">=3.10,<4"

dependencies = [
    "Pillow>=10,<12",
    "numpy>=1.26,<3",
]
